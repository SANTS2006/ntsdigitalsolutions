import { Link, useParams } from 'react-router-dom'; import SEO from '../components/common/SEO'; import insights from '../data/insights';
const body={
 'custom-software-vs-off-the-shelf':['Start with the workflow','Technology should fit the problem. Off-the-shelf tools can be useful when the workflow is standard, while custom software becomes more compelling when your processes, data or approvals are unique.','Before building, map the current process, identify repeated manual work and define what success should look like. A good custom system is not custom for the sake of being different; it is tailored where tailoring creates measurable value.'],
 'digitizing-manual-workflows':['Start with the bottleneck','Digitization works best when you first understand where time, errors and delays occur. Map the people involved, inputs, decisions and outputs before selecting a technology approach.','Then build the smallest useful workflow that removes a real bottleneck. Once the process is stable, the system can grow with reporting, integrations and automation.'],
 'building-secure-business-systems':['Security is a lifecycle','Security starts with requirements and continues through architecture, implementation, testing, deployment and maintenance. The controls needed for a public marketing site are different from those needed for a system handling sensitive organizational data.','At NTS, security conversations should therefore begin early: who can access what, what data is stored, how it is validated, how backups work and how the system will be maintained.']};
export default function InsightDetails(){const {slug}=useParams();const item=insights.find(x=>x.slug===slug);if(!item)return <div className="mx-auto max-w-3xl px-5 py-24">Article not found.</div>;const [h,p1,p2]=body[slug]||['A practical perspective','NTS Digital Solutions shares practical guidance for organizations using technology to improve operations.','The best digital products begin with clear problems, thoughtful design and a delivery process that keeps users and business outcomes in focus.'];return <><SEO title={`${item.title} | NTS Digital Solutions`} description={item.excerpt}/><article className="mx-auto max-w-3xl px-5 py-20"><p className="text-xs font-bold uppercase tracking-widest text-blue-600">{item.category} · {item.date}</p><h1 className="mt-4 text-4xl font-extrabold leading-tight md:text-6xl">{item.title}</h1><p className="mt-6 text-xl leading-8 text-slate-600">{item.excerpt}</p><div className="mt-12 space-y-7 text-lg leading-9 text-slate-700"><h2 className="text-2xl font-extrabold text-slate-950">{h}</h2><p>{p1}</p><p>{p2}</p></div><Link to="/insights" className="mt-10 inline-flex rounded-xl border px-5 py-3 font-bold">Back to Insights</Link></article></>}
