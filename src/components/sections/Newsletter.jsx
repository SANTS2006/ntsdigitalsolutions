import { useState } from 'react';
import { FiMail } from 'react-icons/fi';
export default function Newsletter(){const [email,setEmail]=useState('');const [done,setDone]=useState(false);const submit=e=>{e.preventDefault();if(!email)return;localStorage.setItem('nts-newsletter',email);setDone(true);};return <section className="bg-blue-600 py-16 text-white"><div className="mx-auto flex max-w-7xl flex-col gap-7 px-5 md:flex-row md:items-center md:justify-between lg:px-6"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-blue-100">NTS Insights</p><h2 className="mt-2 text-3xl font-extrabold">Stay ahead of digital innovation.</h2><p className="mt-2 max-w-xl text-blue-100">Get occasional practical ideas on software, digital transformation and technology.</p></div>{done?<div className="rounded-2xl bg-white/10 px-6 py-4 font-semibold">You're on the list. Thank you.</div>:<form onSubmit={submit} className="flex w-full max-w-md gap-2"><label className="sr-only" htmlFor="newsletter-email">Email</label><div className="flex flex-1 items-center rounded-xl bg-white px-4 text-slate-900"><FiMail/><input id="newsletter-email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com" className="w-full bg-transparent px-3 py-3 outline-none"/></div><button className="rounded-xl bg-slate-950 px-5 font-bold">Subscribe</button></form>}</div></section>}
