import { useEffect, useState } from 'react'; import { Link, NavLink } from 'react-router-dom'; import { FiArrowUpRight, FiMenu, FiX } from 'react-icons/fi'; import { AnimatePresence, motion } from 'framer-motion'; import Logo from '../common/Logo'; import SiteSearch from '../common/SiteSearch';
const links=[['Home','/'],['About','/about'],['Services','/services'],['Solutions','/solutions'],['Industries','/industries'],['Insights','/insights']];
export default function Navbar(){const [isOpen,setIsOpen]=useState(false);useEffect(()=>{document.body.style.overflow=isOpen?'hidden':'';return()=>{document.body.style.overflow=''}},[isOpen]);const styles=({isActive})=>`relative py-2 text-sm font-semibold transition-colors ${isActive?'text-blue-600':'text-slate-600 hover:text-slate-950'}`;return <header className="sticky top-0 z-50 border-b border-slate-200/80 bg-white/90 backdrop-blur-xl"><nav aria-label="Main navigation" className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-5 py-4 lg:px-6"><Logo/><div className="hidden items-center gap-5 md:flex">{links.map(([l,to])=><NavLink key={to} to={to} end={to==='/' } className={styles}>{l}</NavLink>)}<SiteSearch/><Link to="/start-a-project" className="inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-bold text-white shadow-lg shadow-blue-600/20 hover:bg-blue-700">Start a Project <FiArrowUpRight/></Link></div><button type="button" onClick={()=>setIsOpen(v=>!v)} aria-label={isOpen?'Close navigation menu':'Open navigation menu'} aria-expanded={isOpen} className="rounded-lg p-2 text-2xl text-slate-800 hover:bg-slate-100 md:hidden">{isOpen?<FiX/>:<FiMenu/>}</button></nav><AnimatePresence>{isOpen&&<motion.div initial={{opacity:0,height:0}} animate={{opacity:1,height:'auto'}} exit={{opacity:0,height:0}} className="border-t border-slate-200 bg-white md:hidden"><div className="mx-auto flex max-w-7xl flex-col px-5 py-5">{links.map(([l,to])=><NavLink key={to} to={to} end={to==='/' } onClick={()=>setIsOpen(false)} className={styles}>{l}</NavLink>)}<div className="mt-3 flex gap-3"><Link to="/faq" onClick={()=>setIsOpen(false)} className="flex-1 rounded-xl border px-4 py-3 text-center font-bold">FAQ</Link><Link to="/start-a-project" onClick={()=>setIsOpen(false)} className="flex-1 rounded-xl bg-blue-600 px-4 py-3 text-center font-bold text-white">Start a Project</Link></div></div></motion.div>}</AnimatePresence></header>}
