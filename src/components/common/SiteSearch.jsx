import { useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import { FiSearch, FiX } from 'react-icons/fi';
const items=[
 ['About NTS','/about','Company'],['Services','/services','Capabilities'],['Solutions','/solutions','Portfolio'],['Industries','/industries','Sectors'],['Case Studies','/case-studies','Proof'],['Insights','/insights','Articles'],['FAQ','/faq','Questions'],['Security','/security','Trust'],['Resources','/resources','Downloads'],['Support','/support','Support'],['Start a Project','/start-a-project','Contact']
];
export default function SiteSearch(){const [open,setOpen]=useState(false);const [q,setQ]=useState('');const results=useMemo(()=>items.filter(x=>x.join(' ').toLowerCase().includes(q.toLowerCase())),[q]);return <>{<button aria-label="Search site" onClick={()=>setOpen(true)} className="rounded-lg p-2 text-xl text-slate-700 hover:bg-slate-100"><FiSearch/></button>}{open&&<div className="fixed inset-0 z-[100] bg-slate-950/80 p-5 backdrop-blur-sm"><div className="mx-auto mt-16 max-w-2xl overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex items-center gap-3 border-b p-5"><FiSearch/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search NTS..." className="flex-1 outline-none"/><button onClick={()=>setOpen(false)} aria-label="Close search"><FiX/></button></div><div className="max-h-[60vh] overflow-y-auto p-3">{results.map(([label,to,desc])=><Link onClick={()=>setOpen(false)} key={to} to={to} className="block rounded-2xl p-4 hover:bg-slate-50"><div className="font-extrabold">{label}</div><div className="text-sm text-slate-500">{desc}</div></Link>)}{!results.length&&<p className="p-5 text-sm text-slate-500">No matching NTS pages found.</p>}</div></div></div>}</>}
